<!--
This template shows the wiki pages that belong to the passed category

Parameters:

 - category := the fully qualified URI of the category (query resolvable)
-->
<div style="float: left; width: 33%; padding: 2px;">
<h2> {{#show: {{{category}}}| rdfs:label}} </h2>

{{#widget: QueryResult | 
 query = '
select ?sp ?order WHERE {
?sp Platform:hasParent {{{category}}}.
OPTIONAL { ?sp Platform:order ?sporder . }
BIND(COALESCE(?sporder, 50) as ?order)
} ORDER BY ?order
'
 | template = '{{{sp}}}'
 | format = {{{renderStyle}}}
 | valueConfiguration = {{ 
      {{ variableName = 'sp'
       | valueResolver = 'DEFAULT_GLOBALREPOSITORY'
      }} }}
 | hideIfEmpty = true
 | repository = 'config'
}}
</div>