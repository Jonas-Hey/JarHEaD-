= Full Snapshots in History =

The following full snapshots are currently available:
 
{{#widget: TableResult |
query = '
SELECT ?snapshotId ?provider ?date
WHERE {
  GRAPH history:MetaContext {
    ?provider rdf:type System:Provider .
    ?provider historyProvider:hasFullSnapshot ?snapshotId .
    ?snapshotId dc:date ?date .
  }
}
ORDER BY DESC(?date)' |
repository = 'history'
}}

