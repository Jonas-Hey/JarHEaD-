= Delta Snapshots in History =
 
The following delta snapshots are currently available:
<br/>
 
{{#widget: TableResult |
query = '
SELECT ?snapshotId ?provider ?date ?associatedFullSnapshot
WHERE {
  GRAPH history:MetaContext {
    ?provider rdf:type System:Provider .
    ?provider historyProvider:hasDeltaSnapshot ?snapshotId .
    ?snapshotId historySnapshot:type history:DeltaSnapshot .
    ?snapshotId dc:date ?date .
    ?snapshotId <http://www.fluidops.com/history/DeltaSnapshot/associatedFullSnapshot> ?associatedFullSnapshot .
  }
}
ORDER BY ASC(?date)' |
repository = 'history'
}}
